services:
  jot-sync:
    build: .
    ports: ["8787:8787"]
    environment:
      JOT_TOKEN: ${JOT_TOKEN:?set a long random token}
    volumes: ["jot-data:/data"]
    restart: unless-stopped
volumes:
  jot-data:
