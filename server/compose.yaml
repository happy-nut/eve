services:
  eve-sync:
    build: .
    ports: ["8787:8787"]
    environment:
      EVE_TOKEN: ${EVE_TOKEN:?set a long random token}
    volumes: ["eve-data:/data"]
    restart: unless-stopped
volumes:
  eve-data:
